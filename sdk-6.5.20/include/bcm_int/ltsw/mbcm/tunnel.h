/*
 * DO NOT EDIT THIS FILE!
 * This file is auto-generated.
 * Edits to this file will be lost when it is regenerated.
 *
 * This license is set out in https://raw.githubusercontent.com/Broadcom-Network-Switching-Software/OpenBCM/master/Legal/LICENSE file.
 * 
 * Copyright 2007-2020 Broadcom Inc. All rights reserved.
 */

#ifndef BCMINT_LTSW_MBCM_TUNNEL_H
#define BCMINT_LTSW_MBCM_TUNNEL_H

#include <bcm/types.h>
#include <bcm/tunnel.h>
#include <bcm_int/ltsw/flexctr.h>
#include <bcm_int/ltsw/tunnel.h>

#include <bcm_int/ltsw/tunnel_int.h>

#include <sal/sal_types.h>

/*!
 * \brief Init tunnel module.
 *
 * \param [in] unit Unit Number.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
typedef int (*tunnel_init_f)(int unit);

/*!
 * \brief Free tunnel module resources.
 *
 * \param [in] unit Unit Number.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
typedef int (*tunnel_free_resource_f)(int unit);

/*!
 * \brief Create a tunnel initiator entry.
 *
 * \param [in] unit Unit Number.
 * \param [in] flags L3 table management flags.
 * \param [in/out] tnl_info L3 tunnel initiator information.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
typedef int (*tunnel_initiator_create_f)(
    int unit,
    uint32_t flags,
    bcm_tunnel_initiator_t *tnl_info);

/*!
 * \brief Delete a tunnel initiator entry.
 *
 * \param [in] unit Unit Number.
 * \param [in] flags L3 table management flags.
 * \param [in] tunnel_id Tunnel ID.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
typedef int (*tunnel_initiator_del_f)(
    int unit,
    uint32_t flags,
    bcm_gport_t tunnel_id);

/*!
 * \brief Get a tunnel initiator entry.
 *
 * \param [in] unit Unit Number.
 * \param [in] intf L3 interface info. (ONLY ifindex used to identify interface).
 * \param [in] tunnel_id Tunnel ID.
 * \param [out] tnl_info Tunnel initiator information.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
typedef int (*tunnel_initiator_get_f)(
    int unit,
    bcm_l3_intf_t *intf,
    bcm_gport_t tunnel_id,
    bcm_tunnel_initiator_t *tnl_info);

/*!
 * \brief Check if an IP tunnel initiator exists in given positon.
 *
 * \param [in] unit Unit Number.
 * \param [in] tunnel_id Tunnel ID.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
typedef int (*tunnel_initiator_used_get_f)(
    int unit,
    bcm_gport_t tunnel_id);

/*!
 * \brief Traverse L3 IP tunnel initiator entries.
 *
 * \param [in] unit Unit Number.
 * \param [in] cb User callback function, called once per valid entry.
 * \param [in] user_data User supplied cookie used in callback function.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
typedef int (*tunnel_initiator_traverse_f)(
    int unit,
    bcm_tunnel_initiator_traverse_cb cb,
    void *user_data);

/*!
 * \brief Set the tunnel initiator property for the given L3 interface.
 *
 * \param [in] unit Unit Number.
 * \param [in] intf L3 interface info. (ONLY ifindex used to identify interface).
 * \param [in] tnl_info Tunnel initiator information.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
typedef int (*tunnel_initiator_set_f)(
    int unit,
    bcm_l3_intf_t *intf,
    bcm_tunnel_initiator_t *tnl_info);

/*!
 * \brief Delete the tunnel association for the given L3 interface.
 *
 * \param [in] unit Unit Number.
 * \param [in] intf L3 interface info. (ONLY ifindex used to identify interface).
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
typedef int (*tunnel_initiator_clear_f)(
    int unit,
    bcm_l3_intf_t *intf);

/*!
 * \brief Add a tunnel terminator entry.
 *
 * \param [in] unit Unit Number.
 * \param [in] tnl_term_info L3 tunnel terminator information.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
typedef int (*tunnel_terminator_add_f)(
    int unit,
    bcm_tunnel_terminator_t *tnl_term_info);

/*!
 * \brief Get a tunnel terminator entry.
 *
 * \param [in] unit Unit Number.
 * \param [in/out] tnl_term_info Lookup key and tunnel terminator information.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
typedef int (*tunnel_terminator_get_f)(
    int unit,
    bcm_tunnel_terminator_t *tnl_term_info);

/*!
 * \brief Delete a tunnel terminator entry.
 *
 * \param [in] unit Unit Number.
 * \param [in/out] tnl_term_info Tunnel terminator parameters.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
typedef int (*tunnel_terminator_delete_f)(
    int unit,
    bcm_tunnel_terminator_t *tnl_term_info);

/*!
 * \brief Traverse L3 tunnel terminator entries.
 *
 * \param [in] unit Unit Number.
 * \param [in] cb User callback function, called once per valid entry.
 * \param [in] user_data User supplied cookie used in callback function.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
typedef int (*tunnel_terminator_traverse_f)(
    int unit,
    bcm_tunnel_terminator_traverse_cb cb,
    void *user_data);

/*!
 * \brief Attach counters entries to the given tunnel.
 *
 * \param [in] unit Unit Number.
 * \param [in] terminator Tunnel terminator.
 * \param [in] info Flex counter information.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
typedef int (*tunnel_terminator_stat_attach_f)(
    int unit,
    bcm_tunnel_terminator_t *terminator,
    bcmi_ltsw_flexctr_counter_info_t *info);

/*!
 * \brief Detach counters entries to the given tunnel.
 *
 * \param [in] unit Unit Number.
 * \param [in] terminator Tunnel terminator.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
typedef int (*tunnel_terminator_stat_detach_f)(
    int unit,
    bcm_tunnel_terminator_t *terminator);

/*!
 * \brief Set flex counter object value for the given tunnel.
 *
 * \param [in] unit Unit Number.
 * \param [in] terminator Tunnel terminator.
 * \param [in] value The flex counter object value.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
typedef int (*tunnel_terminator_flexctr_object_set_f)(
    int unit,
    bcm_tunnel_terminator_t *terminator,
    uint32 value);

/*!
 * \brief Get flex counter object value for the given tunnel.
 *
 * \param [in] unit Unit Number.
 * \param [in] terminator Tunnel terminator.
 * \param [out] value The flex counter object value.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
typedef int (*tunnel_terminator_flexctr_object_get_f)(
    int unit,
    bcm_tunnel_terminator_t *terminator,
    uint32 *value);

/*!
 * \brief Get flex counter information for the given tunnel.
 *
 * \param [in] unit Unit Number.
 * \param [in] terminator Tunnel terminator.
 * \param [out] info The flex counter information.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
typedef int (*tunnel_terminator_flexctr_info_get_f)(
    int unit,
    bcm_tunnel_terminator_t *terminator,
    bcmi_ltsw_flexctr_counter_info_t *info);

/*!
 * \brief Initialize tunnel encap sequence profiles.
 *
 * \param [in] unit Unit Number.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
typedef int (*tunnel_encap_seq_profile_init_f)(int unit);

/*!
 * \brief De-Initialize egress sequence number profile.
 *
 * \param [in] unit Unit Number.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
typedef int (*tunnel_encap_seq_profile_deinit_f)(int unit);

/*!
 * \brief Allocate egress encapsulation sequence number entry.
 *
 * \param [in] unit Unit Number.
 * \param [in] flags Operation flags.
 * \param [in] type Sequence Number type.
 * \param [out] index Sequence number index.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
typedef int (*tunnel_encap_seq_num_alloc_f)(
    int unit,
    uint32_t flags,
    bcmi_ltsw_tunnel_encap_seq_num_type_t type,
    int *index);

/*!
 * \brief Free egress encapsulation sequence number entry.
 *
 * \param [in] unit Unit Number.
 * \param [in] type Sequence Number type.
 * \param [in] index Sequence number index.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
typedef int (*tunnel_encap_seq_num_free_f)(
    int unit,
    bcmi_ltsw_tunnel_encap_seq_num_type_t type,
    int index);

/*!
 * \brief Set egress encapsulation sequence number.
 *
 * \param [in] unit Unit Number.
 * \param [in] idx Index of sequence number table.
 * \param [in] pipe Pipe number.
 * \param [in] number Sequence number.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
typedef int (*tunnel_encap_seq_num_set_f)(
    int unit,
    int idx,
    int pipe,
    uint32_t number);

/*!
 * \brief Get egress encapsulation sequence number.
 *
 * \param [in] unit Unit Number.
 * \param [in] idx Index of sequence number table.
 * \param [in] pipe Pipe number.
 * \param [out] number Sequence number.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
typedef int (*tunnel_encap_seq_num_get_f)(
    int unit,
    int idx,
    int pipe,
    uint32_t *number);

/*!
 * \brief Add egress encapsulation sequence number profile.
 *
 * \param [in] unit Unit Number.
 * \param [in] entry Sequence number profile.
 * \param [out] index Index of sequence number profile.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
typedef int (*tunnel_encap_seq_profile_add_f)(
    int unit,
    bcmi_ltsw_tunnel_encap_seq_profile_t *entry,
    int *index);

/*!
 * \brief Get egress encapsulation sequence number profile.
 *
 * \param [in] unit Unit Number.
 * \param [out] entry Sequence number profile.
 * \param [in] index Index of sequence number profile.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
typedef int (*tunnel_encap_seq_profile_get_f)(
    int unit,
    bcmi_ltsw_tunnel_encap_seq_profile_t *entry,
    int index);

/*!
 * \brief Delete egress encapsulation sequence number profile.
 *
 * \param [in] unit Unit Number.
 * \param [in] index Index of sequence number profile.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
typedef int (*tunnel_encap_seq_profile_delete_f)(
    int unit,
    int index);

/*!
 * \brief Recover egress encapsulation sequence number profile.
 *
 * \param [in] unit Unit Number.
 * \param [in] index Index of sequence number profile.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
typedef int (*tunnel_encap_seq_profile_recover_f)(
    int unit,
    int index);

/*!
 * \brief Check if tunnel type is supported on a device.
 *
 * \param [in] unit Unit Number.
 * \param [in] tnl_type Tunnel type to check.
 * \param [out] tunnel_term Type supported as tunnel terminator.
 * \param [out] tunnel_init Type supported as tunnel initiator.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
typedef int (*tunnel_type_support_check_f)(
    int unit,
    bcm_tunnel_type_t tnl_type,
    int *tunnel_term,
    int *tunnel_init);

/*!
 * \brief Specify tunnel switch control behaviors.
 *
 * \param [in] unit Unit Number.
 * \param [in] control The desired configuration parameter to modify.
 * \param [in] value The value with which to set the parameter.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
typedef int (*tunnel_control_set_f)(
    int unit,
    bcmi_ltsw_tunnel_control_t control,
    uint32_t value);

/*!
 * \brief Retrieve tunnel switch control behaviors.
 *
 * \param [in] unit Unit Number.
 * \param [in] control The desired configuration parameter to retrieve.
 * \param [out] value Pointer to where the retrieved value will be written.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
typedef int (*tunnel_control_get_f)(
    int unit,
    bcmi_ltsw_tunnel_control_t control,
    uint32_t *value);

/*!
 * \brief Set the global tunnel property.
 *
 * \param [in] unit Unit Number.
 * \param [in] tconfig Global information about the L3 tunneling config.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
typedef int (*tunnel_config_set_f)(
    int unit,
    bcm_tunnel_config_t *tconfig);

/*!
 * \brief Get the global tunnel property.
 *
 * \param [in] unit Unit Number.
 * \param [in/out] tconfig Global information about the L3 tunneling config.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
typedef int (*tunnel_config_get_f)(
    int unit,
    bcm_tunnel_config_t *tconfig);

/*!
 * \brief Add tunnel decap port profile.
 *
 * \param [in] unit Unit Number.
 * \param [in] entry Decap port profile.
 * \param [out] index Index of sequence number profile.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
typedef int (*tunnel_decap_port_profile_add_f)(
    int unit,
    bcmi_ltsw_tunnel_decap_port_profile_t *entry,
    int *index);

/*!
 * \brief Get tunnel decap port profile.
 *
 * \param [in] unit Unit Number.
 * \param [out] entry Get tunnel decap port profile.
 * \param [in] index Index of sequence number profile.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
typedef int (*tunnel_decap_port_profile_get_f)(
    int unit,
    bcmi_ltsw_tunnel_decap_port_profile_t *entry,
    int index);

/*!
 * \brief Delete tunnel decap port profile.
 *
 * \param [in] unit Unit Number.
 * \param [in] index Index of sequence number profile.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
typedef int (*tunnel_decap_port_profile_delete_f)(
    int unit,
    int index);

/*!
 * \brief Recover tunnel decap port profile.
 *
 * \param [in] unit Unit Number.
 * \param [in] index Index of sequence number profile.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
typedef int (*tunnel_decap_port_profile_recover_f)(
    int unit,
    int index);

/*!
 * \brief Get tunnel switch control info.
 *
 * \param [in] unit Unit Number.
 * \param [out] sc_info Tunnel switch control info.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
typedef int (*tunnel_sc_info_get_f)(
    int unit,
    const bcmint_tunnel_sc_t **sc_info);

/*!
 * \brief Tunnel driver structure.
 */
typedef struct mbcm_ltsw_tunnel_drv_s {

    /*! Init tunnel module. */
    tunnel_init_f tunnel_init;

    /*! Free tunnel module resources. */
    tunnel_free_resource_f tunnel_free_resource;

    /*! Create a tunnel initiator entry. */
    tunnel_initiator_create_f tunnel_initiator_create;

    /*! Delete a tunnel initiator entry. */
    tunnel_initiator_del_f tunnel_initiator_del;

    /*! Get a tunnel initiator entry. */
    tunnel_initiator_get_f tunnel_initiator_get;

    /*! Check if an IP tunnel initiator exists in given positon. */
    tunnel_initiator_used_get_f tunnel_initiator_used_get;

    /*! Traverse L3 IP tunnel initiator entries. */
    tunnel_initiator_traverse_f tunnel_initiator_traverse;

    /*! Set the tunnel initiator property for the given L3 interface. */
    tunnel_initiator_set_f tunnel_initiator_set;

    /*! Delete the tunnel association for the given L3 interface. */
    tunnel_initiator_clear_f tunnel_initiator_clear;

    /*! Add a tunnel terminator entry. */
    tunnel_terminator_add_f tunnel_terminator_add;

    /*! Get a tunnel terminator entry. */
    tunnel_terminator_get_f tunnel_terminator_get;

    /*! Delete a tunnel terminator entry. */
    tunnel_terminator_delete_f tunnel_terminator_delete;

    /*! Traverse L3 tunnel terminator entries. */
    tunnel_terminator_traverse_f tunnel_terminator_traverse;

    /*! Attach counters entries to the given tunnel. */
    tunnel_terminator_stat_attach_f tunnel_terminator_stat_attach;

    /*! Detach counters entries to the given tunnel. */
    tunnel_terminator_stat_detach_f tunnel_terminator_stat_detach;

    /*! Set flex counter object value for the given tunnel. */
    tunnel_terminator_flexctr_object_set_f tunnel_terminator_flexctr_object_set;

    /*! Get flex counter object value for the given tunnel. */
    tunnel_terminator_flexctr_object_get_f tunnel_terminator_flexctr_object_get;

    /*! Get flex counter information for the given tunnel. */
    tunnel_terminator_flexctr_info_get_f tunnel_terminator_flexctr_info_get;

    /*! Initialize tunnel encap sequence profiles. */
    tunnel_encap_seq_profile_init_f tunnel_encap_seq_profile_init;

    /*! De-Initialize egress sequence number profile. */
    tunnel_encap_seq_profile_deinit_f tunnel_encap_seq_profile_deinit;

    /*! Allocate egress encapsulation sequence number entry. */
    tunnel_encap_seq_num_alloc_f tunnel_encap_seq_num_alloc;

    /*! Free egress encapsulation sequence number entry. */
    tunnel_encap_seq_num_free_f tunnel_encap_seq_num_free;

    /*! Set egress encapsulation sequence number. */
    tunnel_encap_seq_num_set_f tunnel_encap_seq_num_set;

    /*! Get egress encapsulation sequence number. */
    tunnel_encap_seq_num_get_f tunnel_encap_seq_num_get;

    /*! Add egress encapsulation sequence number profile. */
    tunnel_encap_seq_profile_add_f tunnel_encap_seq_profile_add;

    /*! Get egress encapsulation sequence number profile. */
    tunnel_encap_seq_profile_get_f tunnel_encap_seq_profile_get;

    /*! Delete egress encapsulation sequence number profile. */
    tunnel_encap_seq_profile_delete_f tunnel_encap_seq_profile_delete;

    /*! Recover egress encapsulation sequence number profile. */
    tunnel_encap_seq_profile_recover_f tunnel_encap_seq_profile_recover;

    /*! Check if tunnel type is supported on a device. */
    tunnel_type_support_check_f tunnel_type_support_check;

    /*! Specify tunnel switch control behaviors. */
    tunnel_control_set_f tunnel_control_set;

    /*! Retrieve tunnel switch control behaviors. */
    tunnel_control_get_f tunnel_control_get;

    /*! Set the global tunnel property. */
    tunnel_config_set_f tunnel_config_set;

    /*! Get the global tunnel property. */
    tunnel_config_get_f tunnel_config_get;

    /*! Add tunnel decap port profile. */
    tunnel_decap_port_profile_add_f tunnel_decap_port_profile_add;

    /*! Get tunnel decap port profile. */
    tunnel_decap_port_profile_get_f tunnel_decap_port_profile_get;

    /*! Delete tunnel decap port profile. */
    tunnel_decap_port_profile_delete_f tunnel_decap_port_profile_delete;

    /*! Recover tunnel decap port profile. */
    tunnel_decap_port_profile_recover_f tunnel_decap_port_profile_recover;

    /*! Get tunnel switch control info. */
    tunnel_sc_info_get_f tunnel_sc_info_get;

} mbcm_ltsw_tunnel_drv_t;

/*!
 * \brief Set the TUNNEL driver of the device.
 *
 * \param [in] unit Unit Number.
 * \param [in] drv Tunnel driver to set.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
extern int
mbcm_ltsw_tunnel_drv_set(
    int unit,
    mbcm_ltsw_tunnel_drv_t *drv);

/*!
 * \brief Init tunnel module.
 *
 * \param [in] unit Unit Number.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
extern int
mbcm_ltsw_tunnel_init(int unit);

/*!
 * \brief Free tunnel module resources.
 *
 * \param [in] unit Unit Number.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
extern int
mbcm_ltsw_tunnel_free_resource(int unit);

/*!
 * \brief Create a tunnel initiator entry.
 *
 * \param [in] unit Unit Number.
 * \param [in] flags L3 table management flags.
 * \param [in/out] tnl_info L3 tunnel initiator information.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
extern int
mbcm_ltsw_tunnel_initiator_create(
    int unit,
    uint32_t flags,
    bcm_tunnel_initiator_t *tnl_info);

/*!
 * \brief Delete a tunnel initiator entry.
 *
 * \param [in] unit Unit Number.
 * \param [in] flags L3 table management flags.
 * \param [in] tunnel_id Tunnel ID.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
extern int
mbcm_ltsw_tunnel_initiator_del(
    int unit,
    uint32_t flags,
    bcm_gport_t tunnel_id);

/*!
 * \brief Get a tunnel initiator entry.
 *
 * \param [in] unit Unit Number.
 * \param [in] intf L3 interface info. (ONLY ifindex used to identify interface).
 * \param [in] tunnel_id Tunnel ID.
 * \param [out] tnl_info Tunnel initiator information.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
extern int
mbcm_ltsw_tunnel_initiator_get(
    int unit,
    bcm_l3_intf_t *intf,
    bcm_gport_t tunnel_id,
    bcm_tunnel_initiator_t *tnl_info);

/*!
 * \brief Check if an IP tunnel initiator exists in given positon.
 *
 * \param [in] unit Unit Number.
 * \param [in] tunnel_id Tunnel ID.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
extern int
mbcm_ltsw_tunnel_initiator_used_get(
    int unit,
    bcm_gport_t tunnel_id);

/*!
 * \brief Traverse L3 IP tunnel initiator entries.
 *
 * \param [in] unit Unit Number.
 * \param [in] cb User callback function, called once per valid entry.
 * \param [in] user_data User supplied cookie used in callback function.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
extern int
mbcm_ltsw_tunnel_initiator_traverse(
    int unit,
    bcm_tunnel_initiator_traverse_cb cb,
    void *user_data);

/*!
 * \brief Set the tunnel initiator property for the given L3 interface.
 *
 * \param [in] unit Unit Number.
 * \param [in] intf L3 interface info. (ONLY ifindex used to identify interface).
 * \param [in] tnl_info Tunnel initiator information.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
extern int
mbcm_ltsw_tunnel_initiator_set(
    int unit,
    bcm_l3_intf_t *intf,
    bcm_tunnel_initiator_t *tnl_info);

/*!
 * \brief Delete the tunnel association for the given L3 interface.
 *
 * \param [in] unit Unit Number.
 * \param [in] intf L3 interface info. (ONLY ifindex used to identify interface).
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
extern int
mbcm_ltsw_tunnel_initiator_clear(
    int unit,
    bcm_l3_intf_t *intf);

/*!
 * \brief Add a tunnel terminator entry.
 *
 * \param [in] unit Unit Number.
 * \param [in] tnl_term_info L3 tunnel terminator information.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
extern int
mbcm_ltsw_tunnel_terminator_add(
    int unit,
    bcm_tunnel_terminator_t *tnl_term_info);

/*!
 * \brief Get a tunnel terminator entry.
 *
 * \param [in] unit Unit Number.
 * \param [in/out] tnl_term_info Lookup key and tunnel terminator information.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
extern int
mbcm_ltsw_tunnel_terminator_get(
    int unit,
    bcm_tunnel_terminator_t *tnl_term_info);

/*!
 * \brief Delete a tunnel terminator entry.
 *
 * \param [in] unit Unit Number.
 * \param [in/out] tnl_term_info Tunnel terminator parameters.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
extern int
mbcm_ltsw_tunnel_terminator_delete(
    int unit,
    bcm_tunnel_terminator_t *tnl_term_info);

/*!
 * \brief Traverse L3 tunnel terminator entries.
 *
 * \param [in] unit Unit Number.
 * \param [in] cb User callback function, called once per valid entry.
 * \param [in] user_data User supplied cookie used in callback function.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
extern int
mbcm_ltsw_tunnel_terminator_traverse(
    int unit,
    bcm_tunnel_terminator_traverse_cb cb,
    void *user_data);

/*!
 * \brief Attach counters entries to the given tunnel.
 *
 * \param [in] unit Unit Number.
 * \param [in] terminator Tunnel terminator.
 * \param [in] info Flex counter information.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
extern int
mbcm_ltsw_tunnel_terminator_stat_attach(
    int unit,
    bcm_tunnel_terminator_t *terminator,
    bcmi_ltsw_flexctr_counter_info_t *info);

/*!
 * \brief Detach counters entries to the given tunnel.
 *
 * \param [in] unit Unit Number.
 * \param [in] terminator Tunnel terminator.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
extern int
mbcm_ltsw_tunnel_terminator_stat_detach(
    int unit,
    bcm_tunnel_terminator_t *terminator);

/*!
 * \brief Set flex counter object value for the given tunnel.
 *
 * \param [in] unit Unit Number.
 * \param [in] terminator Tunnel terminator.
 * \param [in] value The flex counter object value.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
extern int
mbcm_ltsw_tunnel_terminator_flexctr_object_set(
    int unit,
    bcm_tunnel_terminator_t *terminator,
    uint32 value);

/*!
 * \brief Get flex counter object value for the given tunnel.
 *
 * \param [in] unit Unit Number.
 * \param [in] terminator Tunnel terminator.
 * \param [out] value The flex counter object value.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
extern int
mbcm_ltsw_tunnel_terminator_flexctr_object_get(
    int unit,
    bcm_tunnel_terminator_t *terminator,
    uint32 *value);

/*!
 * \brief Get flex counter information for the given tunnel.
 *
 * \param [in] unit Unit Number.
 * \param [in] terminator Tunnel terminator.
 * \param [out] info The flex counter information.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
extern int
mbcm_ltsw_tunnel_terminator_flexctr_info_get(
    int unit,
    bcm_tunnel_terminator_t *terminator,
    bcmi_ltsw_flexctr_counter_info_t *info);

/*!
 * \brief Initialize tunnel encap sequence profiles.
 *
 * \param [in] unit Unit Number.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
extern int
mbcm_ltsw_tunnel_encap_seq_profile_init(int unit);

/*!
 * \brief De-Initialize egress sequence number profile.
 *
 * \param [in] unit Unit Number.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
extern int
mbcm_ltsw_tunnel_encap_seq_profile_deinit(int unit);

/*!
 * \brief Allocate egress encapsulation sequence number entry.
 *
 * \param [in] unit Unit Number.
 * \param [in] flags Operation flags.
 * \param [in] type Sequence Number type.
 * \param [out] index Sequence number index.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
extern int
mbcm_ltsw_tunnel_encap_seq_num_alloc(
    int unit,
    uint32_t flags,
    bcmi_ltsw_tunnel_encap_seq_num_type_t type,
    int *index);

/*!
 * \brief Free egress encapsulation sequence number entry.
 *
 * \param [in] unit Unit Number.
 * \param [in] type Sequence Number type.
 * \param [in] index Sequence number index.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
extern int
mbcm_ltsw_tunnel_encap_seq_num_free(
    int unit,
    bcmi_ltsw_tunnel_encap_seq_num_type_t type,
    int index);

/*!
 * \brief Set egress encapsulation sequence number.
 *
 * \param [in] unit Unit Number.
 * \param [in] idx Index of sequence number table.
 * \param [in] pipe Pipe number.
 * \param [in] number Sequence number.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
extern int
mbcm_ltsw_tunnel_encap_seq_num_set(
    int unit,
    int idx,
    int pipe,
    uint32_t number);

/*!
 * \brief Get egress encapsulation sequence number.
 *
 * \param [in] unit Unit Number.
 * \param [in] idx Index of sequence number table.
 * \param [in] pipe Pipe number.
 * \param [out] number Sequence number.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
extern int
mbcm_ltsw_tunnel_encap_seq_num_get(
    int unit,
    int idx,
    int pipe,
    uint32_t *number);

/*!
 * \brief Add egress encapsulation sequence number profile.
 *
 * \param [in] unit Unit Number.
 * \param [in] entry Sequence number profile.
 * \param [out] index Index of sequence number profile.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
extern int
mbcm_ltsw_tunnel_encap_seq_profile_add(
    int unit,
    bcmi_ltsw_tunnel_encap_seq_profile_t *entry,
    int *index);

/*!
 * \brief Get egress encapsulation sequence number profile.
 *
 * \param [in] unit Unit Number.
 * \param [out] entry Sequence number profile.
 * \param [in] index Index of sequence number profile.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
extern int
mbcm_ltsw_tunnel_encap_seq_profile_get(
    int unit,
    bcmi_ltsw_tunnel_encap_seq_profile_t *entry,
    int index);

/*!
 * \brief Delete egress encapsulation sequence number profile.
 *
 * \param [in] unit Unit Number.
 * \param [in] index Index of sequence number profile.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
extern int
mbcm_ltsw_tunnel_encap_seq_profile_delete(
    int unit,
    int index);

/*!
 * \brief Recover egress encapsulation sequence number profile.
 *
 * \param [in] unit Unit Number.
 * \param [in] index Index of sequence number profile.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
extern int
mbcm_ltsw_tunnel_encap_seq_profile_recover(
    int unit,
    int index);

/*!
 * \brief Check if tunnel type is supported on a device.
 *
 * \param [in] unit Unit Number.
 * \param [in] tnl_type Tunnel type to check.
 * \param [out] tunnel_term Type supported as tunnel terminator.
 * \param [out] tunnel_init Type supported as tunnel initiator.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
extern int
mbcm_ltsw_tunnel_type_support_check(
    int unit,
    bcm_tunnel_type_t tnl_type,
    int *tunnel_term,
    int *tunnel_init);

/*!
 * \brief Specify tunnel switch control behaviors.
 *
 * \param [in] unit Unit Number.
 * \param [in] control The desired configuration parameter to modify.
 * \param [in] value The value with which to set the parameter.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
extern int
mbcm_ltsw_tunnel_control_set(
    int unit,
    bcmi_ltsw_tunnel_control_t control,
    uint32_t value);

/*!
 * \brief Retrieve tunnel switch control behaviors.
 *
 * \param [in] unit Unit Number.
 * \param [in] control The desired configuration parameter to retrieve.
 * \param [out] value Pointer to where the retrieved value will be written.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
extern int
mbcm_ltsw_tunnel_control_get(
    int unit,
    bcmi_ltsw_tunnel_control_t control,
    uint32_t *value);

/*!
 * \brief Set the global tunnel property.
 *
 * \param [in] unit Unit Number.
 * \param [in] tconfig Global information about the L3 tunneling config.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
extern int
mbcm_ltsw_tunnel_config_set(
    int unit,
    bcm_tunnel_config_t *tconfig);

/*!
 * \brief Get the global tunnel property.
 *
 * \param [in] unit Unit Number.
 * \param [in/out] tconfig Global information about the L3 tunneling config.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
extern int
mbcm_ltsw_tunnel_config_get(
    int unit,
    bcm_tunnel_config_t *tconfig);

/*!
 * \brief Add tunnel decap port profile.
 *
 * \param [in] unit Unit Number.
 * \param [in] entry Decap port profile.
 * \param [out] index Index of sequence number profile.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
extern int
mbcm_ltsw_tunnel_decap_port_profile_add(
    int unit,
    bcmi_ltsw_tunnel_decap_port_profile_t *entry,
    int *index);

/*!
 * \brief Get tunnel decap port profile.
 *
 * \param [in] unit Unit Number.
 * \param [out] entry Get tunnel decap port profile.
 * \param [in] index Index of sequence number profile.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
extern int
mbcm_ltsw_tunnel_decap_port_profile_get(
    int unit,
    bcmi_ltsw_tunnel_decap_port_profile_t *entry,
    int index);

/*!
 * \brief Delete tunnel decap port profile.
 *
 * \param [in] unit Unit Number.
 * \param [in] index Index of sequence number profile.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
extern int
mbcm_ltsw_tunnel_decap_port_profile_delete(
    int unit,
    int index);

/*!
 * \brief Recover tunnel decap port profile.
 *
 * \param [in] unit Unit Number.
 * \param [in] index Index of sequence number profile.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
extern int
mbcm_ltsw_tunnel_decap_port_profile_recover(
    int unit,
    int index);

/*!
 * \brief Get tunnel switch control info.
 *
 * \param [in] unit Unit Number.
 * \param [out] sc_info Tunnel switch control info.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
extern int
mbcm_ltsw_tunnel_sc_info_get(
    int unit,
    const bcmint_tunnel_sc_t **sc_info);

#endif /* BCMINT_LTSW_MBCM_TUNNEL_H */
