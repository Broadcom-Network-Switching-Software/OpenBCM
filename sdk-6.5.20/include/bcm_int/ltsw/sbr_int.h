/*
 * DO NOT EDIT THIS FILE!
 * This file is auto-generated.
 * Edits to this file will be lost when it is regenerated.
 *
 * This license is set out in https://raw.githubusercontent.com/Broadcom-Network-Switching-Software/OpenBCM/master/Legal/LICENSE file.
 * 
 * Copyright 2007-2020 Broadcom Inc. All rights reserved.
 */

#ifndef BCMINT_LTSW_SBR_INT_H
#define BCMINT_LTSW_SBR_INT_H

#include <bcm_int/ltsw/sbr.h>

#include <sal/sal_types.h>
#include <shr/shr_bitop.h>

/*!
 * \brief Strength profile field information.
 */
typedef struct bcmint_sbr_profile_fld_s {

    /*! Field name. */
    const char *name;

    /*! Field value. */
    uint64_t val;

} bcmint_sbr_profile_fld_t;

/*!
 * \brief Strength profile entry information.
 */
typedef struct bcmint_sbr_profile_ent_s {

    /*! Entry index. */
    int index;

    /*! Entry type. */
    bcmi_ltsw_sbr_profile_ent_type_t ent_type;

    /*! Entry key name. */
    const char *key_name;

    /*! Field information array. */
    const bcmint_sbr_profile_fld_t *fld_info;

    /*! Number of field array. */
    int fld_num;

    /*! Shared strength profile entry. */
    struct bcmint_sbr_profile_ent_s *shr_ent;

    /*! Dummy LT operation due to entry sharing. */
    int dummy;

    /*! Control flags. */
    uint32_t flags;

} bcmint_sbr_profile_ent_t;

/*!
 * \brief Strength profile table information.
 */
typedef struct bcmint_sbr_profile_tbl_s {

    /*! Table name. */
    const char *name;

    /*! Entry information array. */
    bcmint_sbr_profile_ent_t *ent_info;

    /*! Number of entry array. */
    int ent_num;

    /*! Table handle for sharing index range. */
    bcmi_ltsw_sbr_profile_tbl_hdl_t shr_hdl;

} bcmint_sbr_profile_tbl_t;

/*!
 * \brief Strength field information.
 */
typedef struct bcmint_sbr_fld_s {

    /*! Field type. */
    bcmi_ltsw_sbr_fld_type_t type;

    /*! Field strength value. */
    uint64_t val;

} bcmint_sbr_fld_t;

/*!
 * \brief Strength field information in a table.
 */
typedef struct bcmint_sbr_tbl_s {

    /*! Strength field information array. */
    const bcmint_sbr_fld_t *fld_info;

    /*! Number of strength field array. */
    int fld_num;

} bcmint_sbr_tbl_t;

/*!
 * \brief Control flags.
 */
/*! Allocate SBR entry index dynamically. */
#define BCMINT_SBR_CTRL_DYNA_ENT_IDX_ALLOC (1 << 0)

/*! Update static SBR entry. */
#define BCMINT_SBR_CTRL_STAT_ENT_UPDATE (1 << 1)

/*! Insert fixed SBR entry index. */
#define BCMINT_SBR_CTRL_FIXED_ENT_IDX (1 << 2)

/*!
 * \brief SBR database.
 */
typedef struct bcmint_sbr_db_s {

    /*! Database to strength Profile table. */
    const bcmint_sbr_profile_tbl_t *profile_tbl_db;

    /*! Bitmap of strength profile tables in DB. */
    SHR_BITDCL *profile_tbl_bmp;

    /*! Database to the tables which have strength field. */
    const bcmint_sbr_tbl_t *tbl_db;

    /*! Bitmap of tables which have strength field. */
    SHR_BITDCL *tbl_bmp;

    /*! Control flags. */
    uint32_t flags;

} bcmint_sbr_db_t;

#endif /* BCMINT_LTSW_SBR_INT_H */
