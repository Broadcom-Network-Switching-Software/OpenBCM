/*
 * DO NOT EDIT THIS FILE!
 * This file is auto-generated.
 * Edits to this file will be lost when it is regenerated.
 *
 * This license is set out in https://raw.githubusercontent.com/Broadcom-Network-Switching-Software/OpenBCM/master/Legal/LICENSE file.
 * 
 * Copyright 2007-2020 Broadcom Inc. All rights reserved.
 */

#ifndef BCMINT_LTSW_UFT_INT_H
#define BCMINT_LTSW_UFT_INT_H

#include <bcm_int/ltsw/uft_int.h>

#include <sal/sal_types.h>
#include <shr/shr_types.h>

/*!
 * \brief UFT string map information.
 */
typedef struct bcmint_uft_str_map_s {

    /*! String name. */
    const char *str;

    /*! Logical table. */
    const char *table;

} bcmint_uft_str_map_t;

/*!
 * \brief Logical table field information.
 */
typedef struct bcmint_uft_field_info_s {

    /*! Logical table field name. */
    const char *field_name;

    /*! Enum symbol. */
    const char *symbol;

} bcmint_uft_field_info_t;

/*!
 * \brief Logical table entry information.
 */
typedef struct bcmint_uft_entry_info_s {

    /*! Entry attribute used to determine which EM group(s) the tile can be assigned to. */
    const char *entry_attr;

    /*! Flowtracker HW learning is supported on this tile. */
    bool hw_learn;

    /*! Count of logical table fields. */
    uint32_t field_cnt;

    /*! Logical table field database. */
    bcmint_uft_field_info_t *field_db;

} bcmint_uft_entry_info_t;

/*!
 * \brief Logical table information.
 */
typedef struct bcmint_uft_table_info_s {

    /*! Count of logical table fields. */
    const char *table_name;

    /*! Count of logical table entries. */
    uint32_t entry_cnt;

    /*! Logical table entry database. */
    bcmint_uft_entry_info_t *entry_db;

} bcmint_uft_table_info_t;

/*!
 * \brief UFT mode information.
 */
typedef struct bcmint_uft_mode_info_s {

    /*! mode. */
    uint32_t mode;

    /*! Count of logical tables. */
    uint32_t table_cnt;

    /*! Logical table database. */
    bcmint_uft_table_info_t *table_db;

} bcmint_uft_mode_info_t;

/*!
 * \brief UFT database.
 */
typedef struct bcmint_uft_db_s {

    /*! The number of array elements in default_lt_array. */
    uint32_t num_lt_array;

    /*! Array of default logical tables. */
    const char **def_lt_array;

    /*! Count of move_depth_map array. */
    uint32_t move_depth_cnt;

    /*! Switch control to TABLE_EM_CONTROL LT key symbol mapping table. */
    shr_enum_map_t *move_depth_map;

    /*! Count of hash_table_map array. */
    uint32_t hash_table_cnt;

    /*! Switch hash table to logical table name mapping table. */
    shr_enum_map_t *hash_table_map;

    /*! Count of bank_id_map array. */
    uint32_t bank_id_cnt;

    /*! Bank ID to physical bank mapping table. */
    shr_enum_map_t *bank_id_map;

    /*! Count of tile_bank_map array. */
    uint32_t tile_bank_cnt;

    /*! Bank ID to physical tile mapping table. */
    shr_enum_map_t *tile_bank_map;

    /*! Count of UFT modes. */
    uint32_t uft_mode_cnt;

    /*! UFT mode database. */
    bcmint_uft_mode_info_t *uft_mode_db;

    /*! Count of UAT modes. */
    uint32_t uat_mode_cnt;

    /*! UAT mode database. */
    bcmint_uft_mode_info_t *uat_mode_db;

    /*! Count of egress UAT modes. */
    uint32_t egress_uat_mode_cnt;

    /*! Egress UAT mode database. */
    bcmint_uft_mode_info_t *egress_uat_mode_db;

    /*! Count of tunnel modes. */
    uint32_t tunnel_mode_cnt;

    /*! Tunnel mode database. */
    bcmint_uft_mode_info_t *tunnel_mode_db;

    /*! Count of VFP modes. */
    uint32_t vfp_mode_cnt;

    /*! VFP mode database. */
    bcmint_uft_mode_info_t *vfp_mode_db;

    /*! Count of property_map array. */
    uint32_t property_cnt;

    /*! Config property to logical table mapping table. */
    bcmint_uft_str_map_t *property_map;

} bcmint_uft_db_t;

/*!
 * \brief Hash tables to logical tables mapping table information.
 */
typedef struct bcmint_uft_table_map_s {

    /*! Logical table. */
    const char *table;

    /*! EM group attribute. */
    int group_attr;

    /*! Hash table. */
    int hash_table;

} bcmint_uft_table_map_t;

/*!
 * \brief UFT variant database.
 */
typedef struct bcmint_uft_var_db_s {

    /*! Bank IDs mapping table. */
    const shr_enum_map_t *bank_id_map;

    /*! Count of Bank IDs mapping table. */
    int bank_id_map_count;

    /*! Group IDs mapping table. */
    const shr_enum_map_t *group_id_map;

    /*! Count of Group IDs mapping table. */
    int group_id_map_count;

    /*! Move depth switch controls to logical table key strings mapping table. */
    const shr_enum_map_t *move_depth_map;

    /*! Count of move depth switch controls to logical table key strings mapping table. */
    int move_depth_map_count;

    /*! Hash tables to logical tables mapping table. */
    const bcmint_uft_table_map_t *table_map;

    /*! Count of hash tables to logical tables mapping table. */
    int table_map_count;

} bcmint_uft_var_db_t;

/*!
 * \brief Get UFT variant database.
 *
 * \param [in] unit Unit Number.
 * \param [out] uft_var_db UFT variant database.
 *
 * \retval SHR_E_NONE No errors.
 * \retval !SHR_E_NONE Failure.
 */
extern int
bcmint_uft_var_db_get(
    int unit,
    bcmint_uft_var_db_t **uft_var_db);

#endif /* BCMINT_LTSW_UFT_INT_H */
