#
# This license is set out in https://raw.githubusercontent.com/Broadcom-Network-Switching-Software/OpenBCM/master/Legal/LICENSE file.
# 
# Copyright 2007-2020 Broadcom Inc. All rights reserved.
#
# Helper make file for setting default library paths.
#

ifndef BCMA
BCMA = $(SDK)/bcma
endif

ifndef BCMMGMT
BCMMGMT = $(SDK)/bcmmgmt
endif

ifndef BCMLU
BCMLU = $(SDK)/linux/bcmlu
endif

ifndef BCMCNET
BCMCNET = $(SDK)/bcmcnet
endif

ifndef BCMPKT
BCMPKT = $(SDK)/bcmpkt
endif

ifndef BCMLT
BCMLT = $(SDK)/bcmlt
endif

ifndef BCMLTM
BCMLTM = $(SDK)/bcmltm
endif

ifndef BCMTRM
BCMTRM = $(SDK)/bcmtrm
endif

ifndef BCMEVM
BCMEVM = $(SDK)/bcmevm
endif

ifndef BCMISSU
BCMISSU = $(SDK)/bcmissu
endif

ifndef BCMTM
BCMTM = $(SDK)/bcmtm
endif

ifndef BCMPTM
BCMPTM = $(SDK)/bcmptm
endif

ifndef BCMDI
BCMDI = $(SDK)/bcmdi
endif

ifndef BCMBD
BCMBD = $(SDK)/bcmbd
endif

ifndef BCMHA
BCMHA = $(SDK)/bcmha
endif

ifndef BCMDRD
BCMDRD = $(SDK)/bcmdrd
endif

ifndef BCMLRD
BCMLRD = $(SDK)/bcmlrd
endif

ifndef BCMLTA
BCMLTA = $(SDK)/bcmlta
endif

ifndef BCMLTX
BCMLTX = $(SDK)/bcmltx
endif

ifndef BCMCTH
BCMCTH = $(SDK)/bcmcth
endif

ifndef BCMLTP
BCMLTP = $(SDK)/bcmltp
endif

ifndef BCMCFG
BCMCFG = $(SDK)/bcmcfg
endif

ifndef BCMLTD
BCMLTD = $(SDK)/bcmltd
endif

ifndef BCMHA
BCMHA = $(SDK)/bcmha
endif

ifndef BCMPMAC
BCMPMAC = $(SDK)/bcmpmac
endif

ifndef BCMFP
BCMFP = $(SDK)/bcmfp
endif

ifndef BCMPC
BCMPC = $(SDK)/bcmpc
endif

ifndef BCMLM
BCMLM = $(SDK)/bcmlm
endif

ifndef BCMECMP
BCMECMP = $(SDK)/bcmecmp
endif

ifndef BCMIMM
BCMIMM = $(SDK)/bcmimm
endif

ifndef PHYMOD
PHYMOD = $(SDK)/phymod
endif

ifndef PCIEPHY
PCIEPHY = $(SDK)/pciephy
endif

ifndef SIM
SIM = $(SDK)/sim
endif

ifndef SHR
SHR = $(SDK)/shr
endif

ifndef SAL
SAL = $(SDK)/sal
endif

ifndef BSL
BSL = $(SDK)/bsl
endif

ifndef BCMSEC
BCMSEC = $(SDK)/bcmsec
endif
